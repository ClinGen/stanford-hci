"""Provide the configuration for the core app."""

from django.apps import AppConfig


class CoreAppConfig(AppConfig):
    """Configure the core app."""

    name = "apps.core"
    verbose_name = "Core Management"
