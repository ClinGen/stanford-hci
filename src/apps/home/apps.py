"""Provide the configuration for the home app."""

from django.apps import AppConfig


class HomeAppConfig(AppConfig):
    """Configure the home app."""

    name = "apps.home"
    verbose_name = "Home Management"
